import { Playground } from '../_components/Playground';
import { Circles2Mock } from './_circles2-mock';

export const metadata = {
  title: 'circles2 — /dev',
};

export default function Circles2MockPage() {
  return (
    <Playground>
      <Circles2Mock />
    </Playground>
  );
}
